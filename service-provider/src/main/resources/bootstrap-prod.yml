# 生产环境
spring:
  application:
    name: service-provider-config
  cloud:
    nacos:
      config:
        server-addr: 192.168.179.138:8848
        file-extension: yaml
  # 多环境配置
  profiles:
    active: prod
